A match 2 memory game.

User enter cell number between 1 to 16 to match two cells with same token.

Example: user key in 3,15 and hit Enter key to guess cell 3 and 15 for a match.

To exit game, user can key in 0,0 and hit Enter.

Game ends when user correctly matches 14 cells. 
